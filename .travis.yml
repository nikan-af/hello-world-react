sudo: required 
language: node_js
node_js:
    - "stable"
    
services: 
    - docker

before_install: 
    - docker build -t wilpat/sample-app -f Dockerfile.dev .
script:     
    - docker run -e CI=true wilpat/sample-app npm run test -- --coverage

deploy:
  provider: elasticbeanstalk
  region: "ap-south-1"
  app: "hello-world-react"
  env: "Helloworldreact-env"
  bucket_name: "elasticbeanstalk-ap-south-1-054384855372"
  bucket_path: "hello-world-react"
  on:
    branch: master
  access_key_id: $AWS_ACCESS_KEY
  secret_access_key: "$AWS_SECRET_KEY"